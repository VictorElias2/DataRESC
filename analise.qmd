---
title: "Análise de salários"
author: "Victor e Jonatas"
format: pdf
editor: visual
---

```{r}

library(tidyverse)

readr::guess_encoding("dados/Remuneracao.csv") # Verifica qual encoding do arquivo

remuneracao <- read_csv2("dados/Remuneracao.csv", locale = locale(encoding = "ISO-8859-2"))

```

## Remuneração salarial de servidores públicos

## Referentes a Junho/25

Resumo principal

```{r, render=F}

summary(remuneracao)

```

You can add options to executable code like this

```{r}
remuneracao %>% 
  select(where(~ all(. == 0, na.rm = F)))
```

The `echo: false` option disables the printing of code (only output is displayed).
